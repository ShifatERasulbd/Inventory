# TODO

- [x] Add SKU inputs to `resources/js/components/product/editForm.jsx` (copy logic from `addForm.jsx`).

- [x] Initialize `form.skus` (or `sku`) in `resources/js/pages/Product/editProduct.jsx` when loading the product.

- [x] Send `skus` (JSON) in `editProduct.jsx` `updateProducts(...)` payload.

- [ ] Quick manual test: open edit product page, enter SKUs per color+size, submit, verify backend payload contains `skus` and DB updates.



